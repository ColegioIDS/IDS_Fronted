# URL base de tu API
NEXT_PUBLIC_API_BASE_URL=https://jcr5xknd-5000.use2.devtunnels.ms

# Configuración de zona horaria
# Usar la zona horaria de Guatemala (UTC-6) para todas las operaciones de fecha
# Esto asegura que los cálculos de día de la semana sean consistentes independientemente
# de la zona horaria del navegador/PC del usuario
NEXT_PUBLIC_TIMEZONE=America/Guatemala

# Clave secreta usada para JWT (debe ser la misma que en el backend)
JWT_SECRET=tu_secreto_super_seguro_minimo_32_caracteres_aqui

# Dominio para la cookie (IMPORTANTE: debe coincidir con backend)
COOKIE_DOMAIN=.devtunnels.ms

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=deheyskt7
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=unsigned_user_profile


